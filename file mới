<!doctype html>
<html lang="vi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Card nhỏ</title>
  <style>
    /* reset nhẹ */
    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: linear-gradient(135deg,#f0f4ff,#fff);
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      padding: 24px;
    }

    .card {
      width: 320px;
      border-radius: 12px;
      box-shadow: 0 8px 24px rgba(20,30,60,0.12);
      background: #fff;
      overflow: hidden;
      transition: transform .18s ease, box-shadow .18s ease;
    }

    .card:hover {
      transform: translateY(-6px);
      box-shadow: 0 16px 40px rgba(20,30,60,0.18);
    }

    .card img {
      width: 100%;
      height: 180px;
      object-fit: cover;
      display: block;
    }

    .card-body {
      padding: 16px;
    }

    .title {
      font-size: 1.1rem;
      font-weight: 600;
      margin-bottom: 8px;
      color: #0b2545;
    }

    .desc {
      font-size: 0.95rem;
      color: #46566a;
      line-height: 1.4;
      margin-bottom: 14px;
    }

    .actions {
      display: flex;
      gap: 8px;
    }

    .btn {
      flex: 1;
      padding: 10px 12px;
      text-align: center;
      border-radius: 10px;
      font-weight: 600;
      cursor: pointer;
      border: none;
      background: linear-gradient(90deg,#3b82f6,#06b6d4);
      color: #fff;
      transition: opacity .12s ease, transform .12s ease;
    }

    .btn:active { transform: scale(.99); }

    .link {
      padding: 10px 12px;
      border-radius: 10px;
      background: transparent;
      border: 1px solid #e6eef9;
      color: #0b2545;
      text-align: center;
      text-decoration: none;
      font-weight: 600;
    }

    /* responsive nhỏ */
    @media (max-width: 360px) {
      .card { width: 100%; }
      .card img { height: 150px; }
    }
  </style>
</head>
<body>

  <article class="card" aria-label="Ví dụ card">
    <img src="https://picsum.photos/600/400?random=1" alt="Ảnh minh họa">
    <div class="card-body">
      <h3 class="title">Tiêu đề mẫu</h3>
      <p class="desc">Đây là một đoạn mô tả ngắn gọn để minh họa cho layout card — gọn, đẹp và responsive.</p>
      <div class="actions">
        <button class="btn" type="button">Hành động</button>
        <a class="link" href="#" role="button">Chi tiết</a>
      </div>
    </div>
  </article>

</body>
</html>
